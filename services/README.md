# services/

Thin deployment entrypoints (Phase 2+): `producer/`, `consumer/`, `api/`, `dashboard/`.
Each wraps library code from `src/churn/`; no business logic lives here.

Empty at Phase 0 (scaffold only).
